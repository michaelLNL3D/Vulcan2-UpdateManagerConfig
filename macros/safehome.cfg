[safe_z_home]
home_xy_position: 183.8, 176.4
speed: 65
# z_hop disabled: the G28 macro (EuclidUtilities.cfg) already does a single
# explicit Z lift after the Y home, before traversing to this probe point.
# A non-zero z_hop here would add a SECOND lift inside G28.1 Z on a re-home.
z_hop: 0